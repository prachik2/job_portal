# -*- coding: utf-8 -*-

def custom_context(request):
    """The main context for the basic values needed for the project."""

    context = {
        'app_title': 'JOB Portal',
    }
    return context
